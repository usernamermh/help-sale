{
  "name": "conversation_query",
  "label": "会话查询",
  "description": "查询会话:view=list 返回最近的会话列表(时间/ID/销售/客户/消息数);view=load 按 conversationId 读取完整对话原文。",
  "category": "会话",
  "parameters": { "type": "object", "properties": { "view": { "type": "string", "enum": ["list", "load"], "default": "list", "description": "查询类型" }, "conversationId": { "type": "string", "description": "会话 ID(load 用)" }, "limit": { "type": "number", "maximum": 100, "description": "列表条数上限" } } },
  "entry": "main.ts",
  "function_list": ["main.execute"]
}