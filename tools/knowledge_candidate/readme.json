{
  "name": "knowledge_candidate",
  "label": "话术候选",
  "description": "管理话术候选:op=list 查看待确认候选;op=approve 确认入库(复用标准分块/去重);op=reject 拒绝。",
  "category": "知识",
  "parameters": { "type": "object", "properties": { "op": { "type": "string", "enum": ["list", "approve", "reject"], "default": "list", "description": "操作类型" }, "candidateId": { "type": "string", "description": "候选 ID(approve/reject 用)" }, "limit": { "type": "number", "maximum": 100, "description": "列表条数上限" } } },
  "entry": "main.ts",
  "function_list": ["main.execute"]
}