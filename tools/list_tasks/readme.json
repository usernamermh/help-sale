{
  "name": "list_tasks",
  "description": "查看待办或已完成的跟进任务。",
  "category": "任务",
  "parameters": { "type": "object", "properties": { "status": { "type": "string", "enum": ["pending", "done"], "description": "pending 或 done" }, "limit": { "type": "number", "default": 20, "maximum": 50 } } },
  "entry": "main.ts",
  "function_list": ["main.execute"]
}