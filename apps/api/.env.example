# 全部运行时配置集中在仓库顶层 help-sale.config.yaml(部署时复制
# help-sale.config.example.yaml 为 help-sale.config.yaml 并填写当前环境的值)。
# 如需使用其他配置文件路径,可通过环境变量 CONFIG_PATH 指定,例如:
# CONFIG_PATH=C:/path/to/help-sale.config.yaml