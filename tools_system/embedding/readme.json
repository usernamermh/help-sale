{
  "name": "embedding",
  "label": "文本向量",
  "description": "把文本转为固定维度向量(本地轻量 BGE 中文模型,约 512 维;模型缺失时自动回退字符 n-gram 向量);供相似度计算与聚类使用。",
  "category": "文本算法",
  "parameters": {
    "type": "object",
    "required": [
      "texts"
    ],
    "properties": {
      "texts": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "要编码的文本列表"
      }
    }
  },
  "entry": "main.ts",
  "function_list": [
    "main.execute"
  ]
}
